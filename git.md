### Ignore file which has already been committed in previous commits
```bash
 git rm --cached [file_name]
```

### git cherry multiple commit
```bash
git cherry-pick A^..D
```

### Git ignore chmod
```bash
git config 
```